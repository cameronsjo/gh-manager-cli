// Re-export the main RepoList component
// Note: The main RepoList.tsx file is still at /src/ui/views/RepoList.tsx
// This index will be used once the file is moved into this directory structure

export { default } from '../RepoList';
